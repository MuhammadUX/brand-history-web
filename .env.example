# Supabase (publishable-by-design keys; RLS protects data)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# AI Profile Builder — dev stub (no external keys; swap to claude/gpt/gemini later)
LLM_PROVIDER=dev
